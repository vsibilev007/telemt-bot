services:
  telemt-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    # Если бот и telemt на одном хосте:
    # network_mode: host
    # Или через внешнюю сеть:
    # networks:
    #   - proxy
    #
    # networks:
    #   proxy:
    #     external: true
